# Shared contracts

The Python package under `python/mamagift_contracts/` contains only the typed Phase 0 AI-worker boundary. It intentionally has no document, parser, embedding, retrieval, or LLM implementation.
