# Compose development environment

The Phase 0 development stack is defined in `docker-compose.yml`. It contains PostgreSQL, the FastAPI API, and the React/Vite web health screen. The API uses an in-process contract-only fake AI worker by default.
