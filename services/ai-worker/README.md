# AI worker boundary

Phase 0 defines the worker protocol in `packages/contracts/python/mamagift_contracts/` and uses an in-process deterministic fake in the API. A separately deployed parser/OCR/LLM worker is later-phase work and must not be added here yet.
